# Server
server.port=8080

# DataSource
# Since Spring Boot and MySQL are on the SAME VM, use localhost
spring.datasource.url=jdbc:mysql://localhost:3306/primaryfeed?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=appuser
spring.datasource.password=PrimaryFeed2024!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.application.name=primaryfeed
spring.jackson.serialization.fail-on-empty-beans=false

# JWT
app.jwt.secret=primaryfeed-super-secret-key-group6-2026-must-be-long-enough
app.jwt.expiration=86400000